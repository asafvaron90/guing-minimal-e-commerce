# test-client-design-system — AI Coding Instructions
# Generated by Guing v0.0.4 on 2026-08-15T00:38:18.177Z
# Base UI library: Material UI (mui-v7.3.11)

## Package import
Always import from `test-client-design-system` — NEVER from the underlying library directly:
```tsx
import { Button, vars } from 'test-client-design-system';
import 'test-client-design-system/src/design-tokens/tokens.css';
```

## Design Token Rules
- NEVER hardcode hex colors — use CSS variables or `vars` references
- NEVER hardcode font sizes or spacing values
- TypeScript access: `import { designTokens, vars } from 'test-client-design-system'`

## vars.* shape (typed CSS-variable references)
- vars.color           — brand500, actionPrimary, actionSecondary, success500, warning500, danger500, background, surface, textMain, textMuted, borderDivider
- vars.colorToken      — backgroundPrimary, backgroundSecondary, backgroundTertiary, backgroundAccent, contentPrimary, contentSecondary, contentTertiary, contentAccent, contentStateDisabled, contentOnColorInverse, contentSale, color000000 … (29 total)
- vars.radius          — small, medium, large, round
- vars.radiusToken     — radiusNone, radiusXS, radiusS, radiusM, radiusL, radiusXL, radius2XL, radiusFull … (10)
- vars.font            — headingHeading1, headingHeading2Extrabold, headingHeading2Semibold, headingHeading2Strikethrough, headingHeading3Extrabold, headingHeading3Bold, headingHeading3Medium, bodyBody1 (each: .family .size .weight .lineHeight)
- vars.spacing         — spaceXXS, spaceXS, spaceS, spaceM, spaceL, spaceXL, space2XL, space3XL, space4XL, space5XL, space6XL, space7XL, space10, space9, space27
- vars.size            — borderNone, borderThin, borderDefault, borderThick, opacityHover, opacityFocus, opacityPressed, opacityDisabled
- vars.shadow          — small, medium, large, inner
- vars.shadowToken     — dropShadow

Usage example:
```tsx
import { vars } from 'test-client-design-system';
<Box sx={{ backgroundColor: vars.color.actionPrimary, borderRadius: vars.radius.medium }} />
```

## Namespace Component API
All Figma-derived components are exported as namespace objects.
Import and use them like this:
```tsx
import { Test } from 'test-client-design-system';

<Test.Card onClick={fn} isLoading={false} />
```

### Standard props (every AI-generated component accepts these)
- `onClick?: () => void` — click handler
- `onHover?: () => void` — mouse-enter handler
- `isDisabled?: boolean` — disables the component
- `isLoading?: boolean` — shows a spinner; disables interaction
- `children?: React.ReactNode` — content slot
- `sx?: Record<string, unknown>` — style overrides (spread last; developer wins)
- `className?: string` — class overrides

## Available Components (1 total)

### TestCard
  TestCard component
  Props:
    - className: string (optional)
    - style: React.CSSProperties (optional)

## Token Summary
- Colors: 29 tokens
- Typography: 15 tokens
- Radius: 10 tokens
- Shadows: 1 tokens
- Gaps: 15 tokens
- Sizes: 9 tokens
- FigmaStyles: 16 tokens

## Component Conventions
- All components use `React.forwardRef`
- Developer overrides always win via `...sx` or `...style` spread
- Do NOT import from `@mui/material` directly — use DS package re-exports
